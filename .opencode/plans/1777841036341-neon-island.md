# Plan: GHCR + VPS env example response

## Goal
Provide a copy-paste `.env` example for VPS deployment that matches the repository's Docker/GHCR pipeline and runtime env validation.

## Recommended approach
1. Confirm the deployment flow from repo config and explain it briefly:
   - GitHub Actions builds and pushes image to GHCR, then triggers Dokploy webhook.
   - VPS runs `compose.yaml` with env substitution.
2. Provide one production-focused env template (for VPS `.env`) with placeholders:
   - Include required vars from `compose.yaml` and `packages/env/src/server.ts`.
   - Include optional/default vars (`WEB_PORT`, `S3_REGION`).
   - Use `WEB_IMAGE=ghcr.io/<owner>/<repo>:latest` placeholder (or repo default image path).
3. Add concise setup notes that are essential for success:
   - GitHub secrets required by workflow: `TOKEN`, `DOKPLOY_WEBHOOK`.
   - If GHCR package is private, VPS/Dokploy must have GHCR pull credentials (`read:packages`).
4. Provide minimal verification steps so the user can validate env correctness before go-live.

## Critical files used as source of truth
- `.github/workflows/dokploy.yml`
- `compose.yaml`
- `packages/env/src/server.ts`
- `.env.docker.example`
- `docs/docker-deploy.md`
- `apps/web/.env.example`

## Planned modifications
- No repository code/config changes required for this request.
- Deliverable is a validated env example and short deployment notes in the assistant response.

## Verification plan
1. **Schema parity check**
   - Ensure every required runtime env key in `packages/env/src/server.ts` is present in the provided env example.
2. **Compose parity check**
   - Ensure required compose substitutions in `compose.yaml` are present (`WEB_IMAGE`, `DATABASE_URL`, auth vars, S3 vars).
3. **Preflight command (user-run on VPS)**
   - `docker compose --env-file .env config` (should render without missing-var errors).
4. **Deploy smoke commands (user-run on VPS)**
   - `docker compose pull web`
   - `docker compose up -d web`
   - `docker compose run --rm web bun run db:migrate`
